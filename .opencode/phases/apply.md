# Apply Phase

You are in the **Apply** phase — code implementation.

## Responsibilities

- Write production-quality code
- Implement the proposed design and architecture
- Follow existing project conventions and patterns
- Use the correct tech stack (C#, .NET 9, EF Core, etc.)
- Apply MeridianUI for all UI work

## Principles

- Write simple, readable code — avoid unnecessary abstractions
- Preserve existing architecture and naming conventions
- Keep changes scoped — don't refactor unrelated code
- Validate inputs at system boundaries
- Use async/await correctly and propagate CancellationToken when natural
- Keep domain models separate from DTOs and ViewModels
- For EF Core: build queries as IQueryable, use projections for reads, AsNoTracking() for queries
- For UI: use MeridianUI tokens and components exclusively

## Output

- Implemented code changes
- Updated files list
- Any deviations from the proposal with justification